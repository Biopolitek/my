text
# --- Обработка изображений и ассетов ---
Pillow==10.2.0          # Работа с PNG/JPG, ресайз, анализ размеров
svgwrite==1.4.3         # Генерация или правка SVG-векторов

# --- Парсинг и логика данных ---
beautifulsoup4==4.12.3  # Анализ index_draft.html и пересборка DOM
lxml==5.1.0             # Быстрый движок для парсинга HTML/XML
PyYAML==6.0.1           # Если используешь YAML для конфигов или маппинга

# --- Работа с путями и системой ---
pathlib; python_version >= "3.4"

# --- Интеграция с ИИ (если запросы идут через API) ---
openai==1.12.0          # Если Декодер общается с GPT напрямую
requests==2.31.0        # Стандарт для HTTP-запросов (Google Fonts, API)

# --- Качество кода (для техлида) ---
flake8==7.0.0           # Линтер (показать, что следишь за чистотой кода)
